import { Reload } from "@/components/globals/Reload";

export default function Loading() {
//global loading state UI, defined and built-in in nextjs, built upon react.suspense()
    return <Reload />
  }